...

/**
 * Insertion Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void insertion_sort(int *v, int size);

/**
 * Selection Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void selection_sort(int *v, int size);

/**
 * Quick Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void quick_sort(int *v, int size);

/**
 * Merge Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void merge_sort(int *v, int size);

/**
 * Bubble Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void bubble_sort(int *v, int size);

/**
 * Shell Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void binary_insertion_sort(int *v, int size);

/**
 * Shell Sort Algorithm
 * @param v Pointer to the first element of the vector.
 * @param size Vector size.
 */
void interpolation_search(int *v, int size);

...